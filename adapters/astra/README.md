# Moved

The portable Spectra skill lives at `plugin/skills/spectra`.

For Codex, copy or symlink that folder to `~/.agents/skills/spectra`.
`spectra run --host astra` still does that link if you already use the CLI.
